# 0) Unzip + commit
unzip foodlabel_ai_SUPER_RELEASE_LOCKED_*.zip -d .
chmod +x commit_super_release.sh scripts/*.sh foodlabel-ai/mobile/scripts/bootstrap_mobile.sh
./commit_super_release.sh --repo-root . --branch main --remote https://github.com/IthaiS/unique.git

# 1) (Optional) Verify integrity
./scripts/verify_bundle.sh .

# 2) Configure GitHub variables/secrets (as documented in README_SUPER.md)

# 3) Infra (idempotent)
# From GitHub Actions UI, run "Infra Stack (Terraform)"

# 4) Tag & push (kicks off release flow if you want)
./scripts/tag_and_push.sh v0.5.0 "Release v0.5.0"

# 5) Deploy by branch pushes
#   develop -> Deploy Backend (dev)
#   main    -> Deploy Backend (prod)
